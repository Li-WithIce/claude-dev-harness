---
tags: [模板, 运行时, 任务]
created: 2026-04-01
updated: 2026-04-01
---

# 任务状态模板

> [!tip] 使用方法
> 创建新的 `运行时/tasks/<task-id>.md` 时，优先从此模板生成。
> 新写入 `stage` 只使用当前 harness 的可写阶段；历史 `SPEC` / `DONE` 仅在 reader 兼容层处理，不再写回新任务文件。

```markdown
---
tags: [运行时, 任务]
created: YYYY-MM-DD
updated: YYYY-MM-DD HH:mm:ss
task_id: <task-id>
task_name: <task-name>
schema_version: task-runtime/v1.1
workspace: <absolute-workspace-path>
writer: Claude Code | Codex | Gemini
---

# <task-id> — <task-name>

## 基本信息

| 项目 | 值 |
|------|-----|
| task_id | `<task-id>` |
| 工作区 | `<absolute-workspace-path>` |
| artifact_root | `docs/<task-id>/` |
| primary_artifact | `docs/<task-id>/<current-main-artifact>.md` |
| schema_version | `task-runtime/v1.1` |
| orchestration | `.assistant/orchestration/current-flow.md` |

## Artifact Links

- primary: `docs/<task-id>/<current-main-artifact>.md`
- related:
  - `docs/<task-id>/spec.md`（optional delta-spec）
  - `docs/<task-id>/plan.md`
  - `docs/<task-id>/implementation-notes.md`
  - `docs/<task-id>/review.md`
  - `docs/<task-id>/test.md`
  - `docs/<task-id>/handoff.md`

## 当前状态

- stage: <INTAKE|PLAN|DEV|REVIEW(implementation)|TEST|HANDOFF>
- runner: <tool>
- 进度: <text>
- 下一步: <text>

## 关键上下文

- <key context>

## 阶段历史

| 时间 | 阶段变化 | 说明 |
|------|----------|------|
| YYYY-MM-DD HH:mm | BOOTSTRAP | <summary> |
```
