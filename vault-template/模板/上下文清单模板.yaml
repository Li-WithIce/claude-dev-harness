# Optional task context manifest. Copy to docs/tasks/<task-id>/context-manifest.yaml when needed.
# Advisory-only: this file does not drive stage, tool, lazy loading, skills_whitelist, or auto injection.
schema_version: context-manifest/v1
summary: ""
contexts:
  - phase: IMPLEMENT
    file: ""
    reason: ""
    required: true
    notes: ""

# forbidden fields:
# - stage
# - status
# - verdict
# - tool
# - current_phase
# - next_action
# - active_task
# - current_pointer
# - skills_whitelist
# - auto_inject
# - injector
# - load_by_default
# - workflow_state
