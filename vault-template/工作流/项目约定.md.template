---
tags: [工作流, 约定, 项目]
created: __RENDER_AT_INSTALL__
updated: __RENDER_AT_INSTALL__
---

# 项目约定

> 这个文件只记录当前工作区稳定、跨任务复用的项目约定。
> 当前任务的临时状态、阶段进度、阻塞项不要写在这里。

## 何时维护

- 当项目有明确的仓库约定、提交流程、验证默认值或交付边界，需要优先于通用 Harness 规则读取时
- 当这些约定会影响多次任务执行，而不只是某一次临时实现时

## 当前项目约定

- 仓库边界：默认一个项目对应一个 WorkspaceRoot；Harness 任务 artifact 统一落在 `docs/tasks/<task-id>/`，当前项目拥有独立的 `.assistant` 运行时状态；canonical workspace entry 统一收口在 `.assistant\entry\`
- 开发默认值：暂无项目特定补充，默认遵循通用 Harness 流程
- 验证默认值：暂无项目特定补充，默认遵循 `plan.md` / `test.md` 契约
- Review 重点：暂无项目特定补充
- 交付约束：暂无项目特定补充

## 维护规则

- 只写稳定约定，不写会话级临时状态
- 若约定已经失效，直接更新本文件，不要在别处保留第二份副本
- 如果某条约定只适用于单个任务，应写回 `docs/tasks/<task-id>/`，而不是这里
