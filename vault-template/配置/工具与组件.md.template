---
tags: [配置, 工具]
created: __RENDER_AT_INSTALL__
updated: __RENDER_AT_INSTALL__
---

# 工具与组件

## 在用工具

| 工具 | 说明 |
|------|------|
| Claude Code | 主开发 agent，记忆系统宿主 |
| Codex | 自主编码 agent |
| Gemini | 测试运行器 |

## 关键路径

| 用途 | 路径 |
|------|------|
| 工作区根目录 | `{WORKSPACE_ROOT}` |
| 记忆目录（Obsidian） | `{VAULT_PATH}` |
| 共享记忆协议 | `{VAULT_PATH}\工作流\共享记忆协议.md` |
| 共享记忆维护入口 | `{REPO_ROOT}\scripts\memory-maintain.ps1 -VaultRoot {VAULT_PATH}` |
| 共享记忆体检入口 | `{REPO_ROOT}\scripts\memory-health.ps1 -VaultRoot {VAULT_PATH}` |
| Gemini 入口 | `{WORKSPACE_ROOT}\GEMINI.md` |
| Workspace AGENTS | `{WORKSPACE_ROOT}\AGENTS.md` |
