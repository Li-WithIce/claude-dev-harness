# TK-03 current Sunset matrix

The baseline is PR #11 Head `2e1949d7bcedc5397404d86a5ce95b51c8dde4a0`.
This is an implementation-progress matrix, not a Release or full Sunset pass.
The current user's confirmed architecture contract replaces the former Stable
dependency; it does not provide missing external owner assertions or authorize
physical removal. Final source/CI bindings are recorded in verification evidence.

| Gate | Status | Current machine evidence | Remaining human or execution evidence |
|---|---|---|---|
| V1S-01 | partial | New/auto v2 creation and explicit-v1 fail-closed tests pass in isolated snapshots. | Product direction confirmed; terminal complete-source and final exact-head CI binding still pending. |
| V1S-02 | partial | The one authorized active legacy identity, dp-03-real-qualification, is now native v2 paused; shared v2 pointer unchanged. | This is not a global workspace inventory. External task owners have not confirmed zero active v1 dependencies. |
| V1S-03 | partial | dp-03 is digest-bound migrated; the two DONE v1 histories are preserved byte-for-byte. | No blanket archive authority; final disposition of all external/remaining eligible histories is not established. |
| V1S-04 | partial | auto/config/invalid-input tests reject v1 and preserve zero-write failures. Pause blocks new work while existing v2 recovery remains available. | Replacement stop-loss accepted by the current user; final exact-head validation binding pending. |
| V1S-05 | partial | Current admission Schema/producer accept v2 only. Historical Decision Schema and digest remain unchanged, not relabeled. | No actual Runtime Default publication or Release owner rollout performed. |
| V1S-06 | partial | Generated entry assets contain only v2 admission/recovery; host and drift tests pass. | Actual installed entry assets are intentionally untouched; external Host owner adoption is unverified. |
| V1S-07 | partial | Desired core/governed/full profiles omit active v1 assets; isolated Distribution/preset and update checks passed. Install/uninstall fixture corrections await the final recheck. | No real installation or fleet distribution acceptance occurred; failed source-A CI remains historical failure. |
| V1S-08 | partial | Locked-history tests prove ordinary v2 status/recovery do not read old plans/pointers; stage/delegation/repair writers reject first. | External operations owners have not confirmed retirement of their v1 recovery dependencies. |
| V1S-09 | partial | 14 exact legacy fixtures are owned archive/not_run; active Memory/transport/migration coverage remains. Five CoreGroup identities are unchanged. | Final full suite and ordinary CI routing/receipt verification pending; archived checks are never counted as passes. |
| V1S-10 | not_met | The one authorized migration completed with independent native dry-run and paused import; the bridge remains explicitly callable. | Zero eligible v1 tasks globally is unproven; retiring the migration tool has no separate user authorization. |

No row is promoted merely because source exists, one local task migrated, an
individual verifier passed, or a date/phase name was reached. Any failed or
unavailable current-head check prevents promotion to `met`.

Physical deletion: **not_run / not authorized**. The adjacent exact stage-body
removal proposal is a bounded first candidate, not permission to apply it and
not a claim that every retained v1 asset can already be removed.
