# TK-03 authorization record

This is a record of the current user's conversation authorization, not an
independent Review Receipt or a Release authorization.

- The user confirmed: `确认按此契约实施 TK-03` after the explicit question about
  replacing the Stable-before-v1-retirement prerequisite.
- The user then answered `授权` to releasing the old v1 current pointer for
  `dp-03-real-qualification` and migrating that task only to v2 `paused`.
- The authorized migration does not execute Qualification, rewrite the original
  plan/Evidence, or modify the existing v2 current pointer. The two DONE v1 task
  histories remain preserved rather than being automatically archived.
- The implementation requires Critical capabilities. Approvals imported into
  the task must state their exact version, Contract and operation scope; this
  record is not a substitute for those machine bindings.
- No physical v1 source deletion is authorized by these messages. That step
  still requires current evidence for all ten Sunset gates and a separately
  confirmed exact removal diff.
