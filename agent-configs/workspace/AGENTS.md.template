# Shared Memory

- Shared memory vault: `{VAULT_PATH}`
- Default language: 中文
- If a task needs recovery, runtime state, long-term preferences, or tool paths, use existing shared memory/runtime docs first; if optional docs are absent, continue from project files and current evidence
- Do not keep parallel runtime notes outside `.assistant`
- 新功能开发或功能修改以当前需求和业务正确性为准；不要为了兼容旧测试、旧快照或过期断言而保留或恢复旧行为。若旧测试与新需求冲突，应更新或删除旧测试并说明原因；只有用户明确要求历史兼容时才保留旧测试兼容。

## Communication

- 默认使用简体中文，除非用户明确要求英文。
- 代码本体保留英文关键字、变量名、函数名、类名、库名。
- 代码讲解、实现思路、报错分析、方案总结使用中文。
- 不泄露密码、密钥、Token、凭证、隐私数据等敏感信息。
- 避免无必要的过程性闲聊；必要进度更新保持简短、事实化。

## Read Order

- Orientation: read existing optional files only if present: `.assistant\首页.md` -> `.assistant\工作流\项目约定.md` -> `.assistant\配置\系统信息.md` -> `.assistant\配置\用户偏好.md` -> `.assistant\配置\工具与组件.md`
- Resume: read existing runtime files only if present: `.assistant\运行时\恢复索引.md` -> `.assistant\运行时\当前任务.md` -> `.assistant\运行时\tasks\<task-id>.md` -> `.assistant\运行时\中断任务.md` -> `.assistant\运行时\上次会话.md`; missing runtime files mean no active recorded state

## Code Understanding

- 修改前先理解仓库结构、已有实现和项目约定。
- 日常找代码、定位文件/符号、自然语言检索、BM25/关键词检索、上下文候选收集，优先使用已暴露且已索引当前项目的 `codedb-mcp`；若环境命名为 `codebase-mcp`，按同一类代码库检索工具处理。
- `codedb-mcp` / `codebase-mcp` 不可用或未索引当前项目时，退回 `rg` / 文件阅读，不阻塞任务。
- 需要 `impact` / `callers` / `callees` / `affected`、跨模块调用链、依赖扩散、架构边界判断等结构化影响分析时，使用 CodeGraph。
- 当任务明确需要 CodeGraph 且 `codegraph_explore` MCP 已暴露时，使用 `codegraph_explore`；否则使用 `codegraph explore "<问题或符号>"`。
- 不因 MCP 已安装而预加载、探测或访问；仅在用户显式点名、任务必需，或本项目规则要求时使用。
- 既有项目未初始化 CodeGraph 时，不擅自初始化；先询问用户。
- CodeGraph 不可用时继续用代码库检索与阅读，并在总结中说明。
- 含代码修改的任务完成后，提示用户是否需要触发 `codegraph sync`；除非用户明确要求，不自动执行 sync。
- 检测到 ponytail 插件可用且任务涉及设计、实现或审查时，可以使用它做过度设计审查或最小实现审查；插件不可用时按最小正确 diff 原则人工执行。

## New Project Bootstrap

- 执行本工作流的新项目初始化时，同步初始化项目工作流、`codedb-mcp` 索引和 CodeGraph。
- 该规则只适用于新项目 bootstrap；既有项目缺失 `codedb-mcp` 或 CodeGraph 时，先询问用户再初始化。
- 初始化失败时说明失败项和原因，不阻塞基础工作流落地。

## Lazy Loading

- After task recognition, follow `.assistant\entry\AGENTS.md` mode rules.
- `quick` loads only entry rules, user preferences / necessary config, and the directly relevant skill.
- `workflow` loads `entry-router`, `orchestrator`, and the current-stage skill.
- `resume-current` / `switch-existing` loads recovery runtime files first, then the current-stage skill.
- `ask` does not load workflow skills; ask one minimal clarification question.
- Clarification-family requests such as 需求澄清 / 需求确认 / 拷问 / 头脑风暴 / 方案压力测试 / 边界确认 follow `.assistant\entry\AGENTS.md` and land in existing `PLAN -> Clarification` when they need durable development decisions; they do not create a new stage.
- Treat unresolved PLAN acceptance, non-goals, impact, rollback/compatibility, or implementation path that cannot guide IMPLEMENT as the same Clarification protocol.
- In that protocol, answer code/doc-discoverable questions first, keep the base Clarification fields, ask remaining user decisions one at a time with a recommended answer, and keep `User Confirmation` draft while any `clarification_ledger` item is `decision: pending`.
- Stage principle routing is only a cognitive lens, not a new five-turn workflow: Entry/Clarification=Socrates, PLAN=Osborn/Hegel/First Principles/Occam, PLAN_REVIEW=Hegel+Bayes, IMPLEMENT=Ponytail/surgical change, CODE_REVIEW=Feynman, TEST=Bayes, revise=Debono value preservation.
- Do not bulk-load all skills, all historical tasks, Claude compatibility skills, or `workflow-team` unless an explicit trigger requires them.

## Task Workflow

- 小任务直接处理，不额外创建计划文档。
- 复杂任务、跨文件修改、风险较高的改动，按现有 workspace 流程执行；PLAN / IMPLEMENT / REVIEW / TEST / SUMMARY 只是执行检查清单，不新增 stage。
- PLAN 明确目标、影响范围、修改方案和验证方式。
- IMPLEMENT 按计划修改，保持改动聚焦。
- REVIEW 自查风险、边界条件、兼容性和安全问题。
- TEST 执行必要测试，或说明无法测试的原因。
- SUMMARY 总结改动、验证结果和后续建议。
- 任务中断后，继续时先恢复目标、已完成事项、待处理事项和验证状态。
- 不为简单命令、报错解释、SQL 改写、配置查询创建额外任务目录。
- 只有长期任务或多轮开发任务才写入 `docs/tasks/<task-id>/`；该目录默认是本地 workflow 产物。
- 任务文档采用追加记录，不覆盖关键历史。

## Memory Writeback

- 非简单任务、跨文件任务、需沿用历史约定时，开始前先检索记忆。
- 用户提到“之前 / 上次 / 记忆 / 经验 / 约定 / 历史决策”时，必须优先查记忆。
- 任务结束后，如发现长期有价值的项目约定、架构决策、排查经验或用户偏好，在总结中提示可沉淀；只有用户明确要求时写入记忆。
- 不写入临时日志、一次性输出、敏感信息。
- 写入前先搜索相近 key；已有则更新，没有再新增。
- key 使用小写短横线，如 `agent-memory-mcp-usage-policy`。
- type 只能用：`architecture`、`pattern`、`feature`、`api`、`bug`、`decision`。
- 若 agentMemory MCP 未暴露，说明可能尚未重启或配置未加载，并继续完成任务。

## Entry Shim

- The full workspace entry instructions live in `.assistant\entry\AGENTS.md`
